import { ZOOM_FACTOR, TILE_SIZE } from "constants/dimensions/game";

export const NOTIFICATION_X = (ZOOM_FACTOR * TILE_SIZE) / 2
export const NOTIFICATION_Y = (ZOOM_FACTOR * TILE_SIZE * 11.4)

export const NOTIFICATION_TIMEOUT = 2000